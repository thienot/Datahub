server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qttg_dwh
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    root: INFO
    com.example.bhxh_api: DEBUG